This project is a Material UI enhanced table built with Vite, React 19, and TanStack Table.

It demonstrates client-side pagination, sorting, global search, add row, and delete row.

- `yarn` and `yarn start` to run and edit the example
- `yarn test` to run the unit test
- `yarn build` to create a production bundle
